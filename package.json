{
  "name": "agent-context-protector",
  "version": "0.10.0",
  "description": "Token/context budget protection for Claude Code. This file exists for the standalone analyze CLI (npx github:ollybaysion/agent-context-protector-plugin); the plugin itself loads via .claude-plugin/plugin.json.",
  "type": "module",
  "private": true,
  "bin": {
    "acp-analyze": "core/analyze/analyze.mjs"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
